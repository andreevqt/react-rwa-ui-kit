
const spacing = 8;

const borderRadius = '16px';

const fontSizeBase = '16px';
const fontFamily = 'Gilroy';

const primary = '#212531';

const colors = {
  base: '#fff',
  primary,
  accent: '#FFCC33',
  accentLight: '#FFD65B',
  accent20: '#F4B700',
  secondary: '#414962',
  secondary20: '#A0AABA',
  bg: primary,
  alert: '#FF2962',
  success: '#62FF29',
};

const uiElements = {
  borderRadius: '20px',
};

export default {
  colors,
  uiElements, 
  spacing,
  borderRadius,
  fontSizeBase,
  fontFamily,
};
